name: Terraform-AWS
on:
    push:
        branches:
            - dev
    pull_request:
env:
    tf_version: 'latest'
    tf_working_dir: '.'

jobs:
    terraform:
        name: 'Terraform check'
        runs-on: ubuntu-latest
        steps:
            - name: 'Checkout'
              uses: actions/checkout@v2
            - name: 'Terraform Setup'
              uses: hashicorp/setup-terraform@v2
              with:
                terraform_version: 1.1.7
            - name: 'Terraform init'
              run: terraform init -backend-config="access_key=${{ secrets.AWS_ACCESS_KEY_ID }}" -backend-config="secert_key=${{ secrets.AWS_SECRET_ACCESS_KEY }}"
              continue-on-error: true
              id: init
            - run: echo ${{ steps.init.outputs.stdout }}
#            - run: echo ${{ steps.init.outputs.stderr }}
#            - run: echo ${{ steps.init.outputs.exitcode }}
#            - name: 'Terraform Validate'
#              id: validate
#              run: terraform validate -no-color
#            - name: 'Terraform Plan'
#              id: plan
#              run: terraform plan -no-color
#              continue-on-error: true
              env:
                GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
                AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
                AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
                TF_VAR_AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
                TF_VAR_AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
                TF_VAR_PUBLIC_KEY: ${{ secrets.PUBLIC_KEY }}
